Исправил всё по последним issue:
- Убрал комментарии
- Отделил реализацию и логику Медиатора
- Медиатор теперь имеет отдельный файл и импортируется в место использования
- Подправил код согласно стайл-гайду


Сделаны все пункты задания. Пояснение по работе:
- Использовал локальную переменную data (Filter.js::data) для вытягивания даты, т.к.
нам говорили не использовать сервер из таски AJAX'a, а так же мы попросту не имеет базы данных,
которую имеют ребята на видео.
- Не использовал данные с Blog Page по причине, описанной выше, однако если в будущем потребуется,
можно переписать функционал рендера постов и на странице Blog и на странице Filter с базы данных
(а пока нам сказали не заморачиваться по поводу data и сосредаточиться на сути задания)
- Добавил комментарии в js'e для лучшего понимания кода
